let word = prompt();
let l = 1;
for(let i = 0; i <= word.length / 2; i++){
  if(word[i] != word[word.length - i - 1]){
    l -= 1;
    break;
  }
}
if(l == 1){
  console.log("YES");
}
else{
  console.log("NO");
}
